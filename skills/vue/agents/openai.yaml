interface:
  display_name: "Vue"
  short_description: "Vue默认配置去重、第三方组件封装与响应式收口规则"
  default_prompt: "使用 $vue 优化 Vue 组件，清理重复默认配置，显式透传第三方属性并用 defineModel 封装双向交互值。"

policy:
  allow_implicit_invocation: true
