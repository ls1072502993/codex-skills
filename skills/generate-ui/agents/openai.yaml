interface:
  display_name: "Generate UI"
  short_description: "当前仓库UI生成与界面代码交付规则"
  default_prompt: "Use $generate-ui to create or modify UI in this repository, keep changes minimal, and do not launch local preview or inspect layout in the built-in browser unless the user explicitly asks."

policy:
  allow_implicit_invocation: true
