interface:
  display_name: "Frontend Global"
  short_description: "当前仓库任意文件改动、链路回查与残留清理约束"
  default_prompt: "在本仓库读取或修改任意文件时使用 $frontend-global，完整执行该技能中的全部适用规则，并在完成前回查变更链路、清理失效中转逻辑。"

policy:
  allow_implicit_invocation: true
